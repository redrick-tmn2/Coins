<mahApps:MetroWindow x:Class="CoinsApplication.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:CoinsApplication.Views.Controls"
        xmlns:mahApps="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        mc:Ignorable="d"
        Title="Coins" Height="600" Width="800"
      
        ShowIconOnTitleBar="True"
                     
        BorderThickness="0" 
        GlowBrush="Black"
        ResizeMode="CanResizeWithGrip"

        WindowTransitionsEnabled="False"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/CoinsApplication;component/Resources/Coin-us-dollar-icon.png"
        
        DataContext="{Binding MainWindowViewModel, Source={StaticResource ViewModelLocator}}"
        d:DataContext="{Binding MainWindowViewModel, Source={StaticResource ViewModelLocator}}">
    <mahApps:MetroWindow.Flyouts>
        <mahApps:FlyoutsControl>
            <mahApps:Flyout x:Name="EditFlyout" Header="Edit the coin" Position="Right" Width="300">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                        <RowDefinition Height="Auto"></RowDefinition>
                    </Grid.RowDefinitions>

                    <controls:ImageControl DataContext="{Binding SelectedProfile}" d:DataContext="{Binding SelectedProfile}"/>

                    <Label Grid.Row="1" Content="Title:" Style="{DynamicResource DescriptionHeaderStyle}" />
                    <TextBox Grid.Row="2" Margin="{StaticResource DefaultThickness}" Text="{Binding SelectedProfile.SelectedCoin.Title}"></TextBox>

                    <Label Grid.Row="3" Content="Currency:" Style="{DynamicResource DescriptionHeaderStyle}" />
                    <ComboBox Grid.Row="4" Margin="{StaticResource DefaultThickness}" ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedProfile.SelectedCoin.Currency}" ItemTemplate="{StaticResource CurrencyItemTemplate}"></ComboBox>

                    <Label Grid.Row="5" Content="Country:" Style="{DynamicResource DescriptionHeaderStyle}" />
                    <ComboBox Grid.Row="6" Margin="{StaticResource DefaultThickness}" ItemsSource="{Binding Countries}" SelectedItem="{Binding SelectedProfile.SelectedCoin.Country}" ItemTemplate="{StaticResource CountryItemTemplate}"></ComboBox>
                </Grid>
            </mahApps:Flyout>
        </mahApps:FlyoutsControl>
    </mahApps:MetroWindow.Flyouts>
    
    <Grid Margin="{StaticResource DefaultThickness}">
        <Grid.ColumnDefinitions>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Grid Margin="0 0 0 2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <ComboBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Model.Name" HorizontalAlignment="Left" VerticalAlignment="Center" MinWidth="200" Height="16">
            </ComboBox>

            <Button Grid.Column="1" Style="{DynamicResource MetroCircleButtonStyle}" 
                    Width="40" Height="40" HorizontalAlignment="Right" Click="OnEditClick">
                <Rectangle Width="16"
                           Height="16"
                           Fill="{Binding Path=Foreground, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Button}}}">
                    <Rectangle.OpacityMask>
                        <VisualBrush Stretch="Fill" Visual="{DynamicResource appbar_edit}" />
                    </Rectangle.OpacityMask>
                </Rectangle>
            </Button>
        </Grid>

        <ListView x:Name="ListView" Grid.Row="1" ItemsSource="{Binding SelectedProfile.Model.Coins}" SelectedItem="{Binding SelectedProfile.SelectedCoin}" DisplayMemberPath="Title" >
            <ListView.View>
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Title" DisplayMemberBinding="{Binding Title}"></GridViewColumn>
                        <GridViewColumn Header="Currency" DisplayMemberBinding="{Binding Currency.Title}"></GridViewColumn>
                        <GridViewColumn Header="Country" DisplayMemberBinding="{Binding Country.Title}"></GridViewColumn>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</mahApps:MetroWindow>
