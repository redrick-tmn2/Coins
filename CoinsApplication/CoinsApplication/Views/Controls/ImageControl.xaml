<UserControl x:Class="CoinsApplication.Views.Controls.ImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <Label Content="Image:" Style="{DynamicResource DescriptionHeaderStyle}" />

        <StackPanel Orientation="Horizontal">
            <Image Height="100" Width="100" HorizontalAlignment="Center" 
               Margin="{StaticResource DefaultThickness}" 
               Source="{Binding SelectedCoin.Image, Converter={StaticResource ByteArrayToImageSourceConverter}}"/>

            <Button HorizontalAlignment="Center" 
                Style="{DynamicResource AccentedSquareButtonStyle}" 
                Command="{Binding UpdateCoinImageCommand}" 
                CommandParameter="{Binding SelectedCoin}">Browse...</Button>
            
        </StackPanel>
    </StackPanel>
</UserControl>
